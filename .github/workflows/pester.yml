name: Pester Tests & Coverage

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# Permissions required for test-reporter (checks) and PR coverage comment (pull-requests)
permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  pester:
    name: Run Pester Tests
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Install Pester using Windows PowerShell 5.1 (matches the script's minimum requirement)
      - name: Install Pester
        shell: powershell
        run: |
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module -Name Pester -MinimumVersion 5.0.0 -Force -Scope CurrentUser -SkipPublisherCheck

      # Ensure the coverage output directory exists before Pester tries to write to it
      - name: Create coverage directory
        shell: powershell
        run: New-Item -ItemType Directory -Force -Path coverage | Out-Null

      # Run tests with code coverage using New-PesterConfiguration (Pester v5 recommended approach)
      - name: Run Pester tests with code coverage
        shell: powershell
        env:
          TEST_MODE: "1"
        run: |
          $config = New-PesterConfiguration

          # Test execution
          $config.Run.Path     = ".\tests"
          $config.Run.PassThru = $true

          # Code coverage — scoped to the single production script
          $config.CodeCoverage.Enabled      = $true
          $config.CodeCoverage.Path         = ".\entra-id-pre-provision-keys-onespan-fx7.ps1"
          $config.CodeCoverage.OutputFormat = "JaCoCo"
          $config.CodeCoverage.OutputPath   = "coverage\coverage.xml"

          # Test result report (JUnit XML consumed by dorny/test-reporter)
          $config.TestResult.Enabled      = $true
          $config.TestResult.OutputFormat = "JUnitXml"
          $config.TestResult.OutputPath   = "test-results.xml"

          $config.Output.Verbosity = "Normal"

          $results = Invoke-Pester -Configuration $config

          if ($results.FailedCount -gt 0) {
            Write-Error "Pester: $($results.FailedCount) test(s) failed."
            exit 1
          }

      # Publish test results to the Checks tab on the PR / commit
      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Pester Test Results
          path: test-results.xml
          reporter: java-junit

      # Upload coverage XML and JUnit XML as artifacts for the reporting job
      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pester-test-artifacts
          path: |
            coverage/coverage.xml
            test-results.xml
          retention-days: 30

  coverage:
    name: Coverage Report
    runs-on: ubuntu-latest
    needs: pester
    if: always()

    steps:
      # Download the artifacts produced by the pester job
      # path: . ensures files are extracted to the workspace root preserving directory structure
      - name: Download test artifacts
        uses: actions/download-artifact@v4
        with:
          name: pester-test-artifacts
          path: .

      # Pester's JaCoCo output includes a <!DOCTYPE> declaration that references
      # an external DTD irongut cannot resolve inside Docker. Strip it first.
      - name: Strip DOCTYPE from coverage XML
        run: sed -i '/<!DOCTYPE/d' coverage/coverage.xml

      # Parse the JaCoCo XML and write a coverage table to the job summary
      # Also writes code-coverage-results.md for the PR comment step below
      # Container actions require Linux — this job runs on ubuntu-latest
      - name: Generate coverage summary
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: coverage/coverage.xml
          badge: true
          fail_below_min: true
          format: markdown
          hide_branch_rate: false
          hide_complexity: true
          indicators: true
          output: both
          # Warning threshold: 70 %, failure threshold: 90 %
          thresholds: "70 90"

      # Sticky comment on the PR that updates in place on each new push
      - name: Post coverage comment to PR
        uses: marocchino/sticky-pull-request-comment@v2
        if: github.event_name == 'pull_request'
        with:
          recreate: true
          path: code-coverage-results.md
